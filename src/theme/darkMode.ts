type colorTypes = {
  primaryColor: string;
  secondaryColor: string;
  primaryBgColor: string;
  secondaryBgColor: string;
};

export const darkMode: colorTypes = {
  primaryColor: "orange",
  secondaryColor: "darkblue",
  secondaryBgColor: "",
  primaryBgColor: "",
};

export const lightMode: colorTypes = {
  primaryColor: "orange",
  secondaryColor: "darkblue",
  secondaryBgColor: "",
  primaryBgColor: "",
};
